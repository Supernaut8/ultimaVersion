import { getCharacter, getCharacters, getEpisodeWithLink } from "./services/getData.js";

const inputSearch = document.getElementById(inputSearch);
inputSearch.addEventListener("click", async (e) => {
    coincidencias(argumento);


    function coincidencias(argumento) {
        for (let i=0; i<argumento.length - 1, i++) {
            if ( argumento[i].split == )
        }
    }
})

